# -*- coding: utf-8 -*-
"""
citation_map.py — 针对当前稿件的引文映射判断表（每次处理新稿时由 Claude 生成并覆盖本文件）。

这是「混合策略」中的判断环节产物：Claude 依据 references/新闻与传播研究_格式与注释规范.md
逐条把原脚注/括注映射到目标格式，apply_citations.py 读取本表并用 footnote_ops 写回。

铁律：每条目标只重排既有信息的顺序与标点；缺失项一律进 FLAGS 待人工复核，绝不编造。
外文题名用 (文本, True) 段写成斜体；其余 (文本, False)。

注：本文件为示例模板，使用虚构文献。处理真实稿件时，Claude 依据 extract_structure.py
输出重新生成此文件的完整内容。

--------------------------------------------------------------------------
使用说明（新稿处理流程）：
  Step 1  python3 scripts/extract_structure.py "<输入.docx>"
  Step 2  Claude 读 Step 1 输出 + references/格式规范，生成新的 citation_map.py（覆盖本文件）
  Step 3  python3 scripts/apply_all.py "<输入.docx>"
--------------------------------------------------------------------------
"""

# ── 1) 现有脚注 → 新传研究格式（segments: [(text, italic), ...]） ─────────────
# 仅列需要改写的脚注；已合规脚注无需出现在此表。
# 格式说明：
#   中文著作：责任者：《书名》，出版地：出版者，年份，第X页。
#   中文期刊：责任者：《篇名》，《刊名》年份第X期。
#   中文报纸：责任者：《篇名》，《报名》年月日，第X版。
#   外文专著：名 姓, [italic]Title[/italic], City: Publisher, Year, p. X.
#   外文期刊：姓, 名, "篇名," [italic]刊名[/italic], vol. X, no. X, Year, pp. X-X.
#   译著（中）：〔国〕作者：《书名》，译者名译，出版地：出版者，年份，第X页。

FOOTNOTE_TARGETS = {
    # 示例①：中文期刊（原格式为 APA 括注 + 脚注混用，重排为本刊体例）
    2: [("张三：《传播研究的范式转换》，《新闻与传播研究》2020年第5期。", False)],

    # 示例②：外文专著（名前姓后，题名斜体）
    3: [("Li Si, ", False),
        ("Media Memory and the Public Sphere", True),
        (", Beijing: Peking University Press, 2019, p. 45.", False)],

    # 示例③：中文译著（需加国籍标注）
    4: [("〔美〕约翰·杜威：《经验与自然》，傅统先译，北京：商务印书馆，2019年，第30页。", False)],

    # 示例④：外文期刊析出（姓前名后，篇名英文引号，刊名斜体，无具体页码则去页码）
    5: [("Wang, Ming, "Digital Memory and Cultural Identity," ", False),
        ("Journal of Communication", True),
        (", vol. 70, no. 2, 2021, pp. 110-130.", False)],

    # 示例⑤：中文文集析出
    6: [("李四：《记忆与认同》，载王五主编：《传播研究新视野》，上海：复旦大学出版社，2022年，第88页。", False)],
}

# ── 2) 需删除的脚注（空壳/无引用） ────────────────────────────────────────────
# 注意：若某条空脚注在 settings.xml 中被 footnotePr 引用，不要在此列出（会触发 Word 报错）；
# 留在 Word 中手动删除，并在 FLAGS 中提示。
DELETE_FN = []

# ── 3) 括注（夹注）动作 ──────────────────────────────────────────────────────
# action 取值：
#   delete       删除括注（与同段脚注重复）
#   to_footnote  新建脚注（fn_text 或 fn_segments）并在原位插引用标记，再删括注
#   keep_flag    保留括注，仅在报告标记待复核（信息缺失或有疑点）
# para 为 extract_structure.py 的段落索引（0 起）；cite 为精确括注字符串。
INTEXT_ACTIONS = [
    # —— 田野/访谈/资料类 → 转脚注 ——
    {"para": 5,  "cite": "（20230615，田野笔记）",    "action": "to_footnote", "fn_text": "田野笔记，2023年6月15日。"},
    {"para": 12, "cite": "（20231020，村民访谈资料）", "action": "to_footnote", "fn_text": "村民访谈资料，2023年10月20日。"},

    # —— 与同段脚注重复的 APA/学术括注 → 删除 ——
    {"para": 8,  "cite": "（张三，2020）",            "action": "delete"},
    {"para": 15, "cite": "（Wang，2021）",             "action": "delete"},

    # —— 重复引用（原位无脚注）→ 新建完整重复脚注；报告建议改"同注X" ——
    {"para": 20, "cite": "（约翰·杜威，2019:88）", "action": "to_footnote",
     "fn_text": "〔美〕约翰·杜威：《经验与自然》，傅统先译，北京：商务印书馆，2019年，第88页。"},

    # —— 外文斜体脚注（用 fn_segments 而非 fn_text）——
    {"para": 25, "cite": "（Li，2019）", "action": "to_footnote",
     "fn_segments": [("Li Si, ", False),
                     ("Media Memory and the Public Sphere", True),
                     (", Beijing: Peking University Press, 2019, p. 102.", False)]},

    # —— 信息缺失/疑点：保留括注，报告标记待补 ——
    {"para": 30, "cite": "（王六，2018）", "action": "keep_flag"},
]

# ── 4) 待人工复核清单（写入修改报告第四节） ────────────────────────────────────
# 分类：匿名 / 缺信息 / 冲突 / 重复 / 人名 / 格式
FLAGS = [
    # 匿名
    ("匿名", "请确认正文中无作者姓名、单位、项目编号等身份信息（含致谢段落）；资助说明置于文末且仅保留一项。"),

    # 缺信息
    ("缺信息", "fn3（Li Si）缺出版地，已按铁律保留原有信息，未补全。"),
    ("缺信息", "P30 括注（王六，2018）无对应脚注，缺著录信息，已保留待补。"),

    # 重复
    ("重复", "fn4（约翰·杜威《经验与自然》）在 P20 再次引用（页码不同），已新建完整重复脚注，"
             "建议改为「同注X，第Y页」（分页后人工填写当页序号）。"),

    # 人名
    ("人名", "外国学者首次出现时请确认已按规范标注「汉译（原名）」，如「约翰·杜威（John Dewey）」。"),

    # 格式
    ("格式", "期刊要求每页 38 行 × 每行 41 字，脚本已按 A4 + 标准页边距设置；最终行数以 Word 重排为准。"),
    ("格式", "英文标题/作者/摘要/关键词：本 skill 不自动翻译生成，请另附。"),
]
